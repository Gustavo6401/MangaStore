@if (ViewData["Success"] != null)
{
    <div class="alert alert-success notification">
        @ViewData["Success"]
    </div>
}

@if (ViewData["Error"] != null)
{
    <div class="alert alert-success notification">
        @ViewData["Error"]
    </div>
}